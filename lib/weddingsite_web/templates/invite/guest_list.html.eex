<h1>Guest List for <%= @invite.family_name %></h1>

<div><%= @invite.code %></div>

<%= render "guest_list_form.html", Map.put(assigns, :action, Routes.invite_path(@conn, :update_guests, @invite)) %>
